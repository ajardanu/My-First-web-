const nav3 = document.querySelector('.menu nav ul li a');
document.querySelectorAll('.menu nav ul li a[href^="#"]').forEach((anchor) => {
  anchor.addEventListener('click', function (e) {
    e.preventDefault();
    document.querySelector(this.getAttribute('href')).scrollIntoView({
      behavior: 'smooth',
    });
  });
});